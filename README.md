# Stage 5I MTMD Symbol Dump

This update is diagnostic only. It builds the Android arm64-v8a shared libraries
and uploads symbol dump text files so we can identify the actual exported mtmd
API names in the current llama.cpp build.

Do not expect real screenshot inference yet.
