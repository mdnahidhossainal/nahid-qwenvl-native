# Stage 5R — Bengali Screen Summary Prompt Probe

Improves Stage 5Q output quality by keeping the successful vision embedding + greedy generation path and changing only the prompt to request a useful Bengali screen summary.
